name: build-lv2

on:
  push:
    paths:
      - 'effects/**'
      - 'dsp/**'
      - 'src/dsp/**'
      - '.github/workflows/build-lv2.yml'
  pull_request:
    paths:
      - 'effects/**'
      - 'dsp/**'
      - 'src/dsp/**'
      - '.github/workflows/build-lv2.yml'
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  home-fx-lv2:
    runs-on: ubuntu-24.04-arm
    timeout-minutes: 40
    steps:
      - uses: actions/checkout@v4

      - name: Install Faust (host codegen only -- see the cross-compile step below)
        timeout-minutes: 8
        run: |
          sudo apt-get update
          sudo apt-get install -y faust lv2-dev pkg-config build-essential libboost-dev

      - name: Verify the vendored Faust chain from dubfx
        run: |
          if [ -d effects/home/faust ]; then
            echo "using vendored Faust chain under effects/home/faust"
            ls -la effects/home/faust
          else
            echo "::error::effects/home/faust not present — vendor the dubfx chain first"
            exit 1
          fi

      - name: Build the full home stack (loop engine + effects) as a musl/aarch64 LV2
        timeout-minutes: 25
        run: |
          set -e
          FAUSTLIB=$(dirname "$(ls -f /usr/share/faust/lv2ui.cpp /usr/local/share/faust/lv2ui.cpp 2>/dev/null | tail -1)")
          mkdir -p /tmp/homestack
          cp dsp/loop.dsp dsp/aloop.dsp dsp/effects_runtime.dsp /tmp/homestack/
          cp effects/home/faust/*.dsp effects/home/faust/*.h /tmp/homestack/
          sed -i 's#effects/home/faust/##g' /tmp/homestack/effects_runtime.dsp
          sed -i 's#\.\./effects/home/faust/chain\.dsp#chain.dsp#' /tmp/homestack/aloop.dsp
          cd /tmp/homestack
          CLSNAME=aloop
          WORK=/tmp/lv2work
          mkdir -p "$WORK/$CLSNAME.lv2"

          faust -i -a "$FAUSTLIB/lv2.cpp" -vec -fun -dfs -vs 32 -nvi -ct 0 -cn "$CLSNAME" aloop.dsp -o "$WORK/$CLSNAME.cpp"

          CPPFLAGS="-DPLUGIN_URI=\"https://faustlv2.bitbucket.io/$CLSNAME\" -DFAUST_META=1 -DFAUST_MIDICC=1 -DFAUST_MTS=1 -DFAUST_UI=0 -DVOICE_CTRLS=1 -DNVOICES=-1"

          g++ -std=c++11 -fvisibility=hidden -O3 -DDLLEXT="\".so\"" $CPPFLAGS \
            -I"$(pwd)" "$WORK/$CLSNAME.cpp" -o "$WORK/$CLSNAME.meta"
          "$WORK/$CLSNAME.meta" > "$WORK/$CLSNAME.lv2/$CLSNAME.ttl"

          printf '%s\n' \
            '@prefix doap: <http://usefulinc.com/ns/doap#> .' \
            '@prefix foaf: <http://xmlns.com/foaf/0.1/> .' \
            '@prefix lv2:  <http://lv2plug.in/ns/lv2core#> .' \
            '@prefix rdfs: <http://www.w3.org/2000/01/rdf-schema#> .' \
            '' \
            "<https://faustlv2.bitbucket.io/$CLSNAME>" \
            '    a lv2:Plugin ;' \
            "    lv2:binary <$CLSNAME.so> ;" \
            "    rdfs:seeAlso <$CLSNAME.ttl> ." \
            > "$WORK/$CLSNAME.lv2/manifest.ttl"

          cp "$WORK/$CLSNAME.cpp" .
          docker run --rm --platform linux/arm64 -v "$PWD":/w -w /w \
            -e CPPFLAGS="$CPPFLAGS" -e CLSNAME="$CLSNAME" \
            alpine:3.20 sh -c '
              set -e
              apk add --no-cache build-base lv2-dev boost-dev
              g++ -shared -std=c++11 -fvisibility=hidden -O3 -mcpu=cortex-a72 -fPIC -DDLLEXT="\".so\"" $CPPFLAGS \
                -I. "$CLSNAME.cpp" -o "$CLSNAME.so"
              file "$CLSNAME.so"
            '
          cp "$CLSNAME.so" "$WORK/$CLSNAME.lv2/"

          ls -R "$WORK/$CLSNAME.lv2" || (echo "::error::no .lv2 bundle produced" && exit 1)
          mkdir -p "$GITHUB_WORKSPACE/effects/home"
          cp -r "$WORK/$CLSNAME.lv2" "$GITHUB_WORKSPACE/effects/home/"

      - name: Validate the LV2 bundle
        run: |
          sudo apt-get install -y lv2-dev lilv-utils || true
          cd effects/home
          find . -name '*.ttl' -print
          if command -v lv2_validate >/dev/null; then
            lv2_validate $(find . -name 'manifest.ttl') || echo "::warning::lv2_validate reported issues"
          fi

      - name: Upload home-FX LV2 bundle
        uses: actions/upload-artifact@v4
        with:
          name: home-fx-lv2
          path: effects/home/*.lv2
          if-no-files-found: error
          retention-days: 3

  guitar-lofi-fx-lv2:
    runs-on: ubuntu-24.04-arm
    timeout-minutes: 40
    steps:
      - uses: actions/checkout@v4

      - name: Install Faust (host codegen only -- see the cross-compile step below)
        timeout-minutes: 8
        run: |
          sudo apt-get update
          sudo apt-get install -y faust lv2-dev pkg-config build-essential libboost-dev

      - name: Build guitar_lofi_fx.dsp as its own musl/aarch64 LV2 bundle
        timeout-minutes: 15
        run: |
          set -e
          FAUSTLIB=$(dirname "$(ls -f /usr/share/faust/lv2ui.cpp /usr/local/share/faust/lv2ui.cpp 2>/dev/null | tail -1)")
          cd effects/home/faust
          CLSNAME=guitar_lofi_fx
          WORK=/tmp/lv2work
          mkdir -p "$WORK/$CLSNAME.lv2"

          faust -i -a "$FAUSTLIB/lv2.cpp" -vec -fun -dfs -vs 32 -nvi -ct 0 -cn "$CLSNAME" "$CLSNAME.dsp" -o "$WORK/$CLSNAME.cpp"

          CPPFLAGS="-DPLUGIN_URI=\"https://faustlv2.bitbucket.io/$CLSNAME\" -DFAUST_META=1 -DFAUST_MIDICC=1 -DFAUST_MTS=1 -DFAUST_UI=0 -DVOICE_CTRLS=1 -DNVOICES=-1"

          g++ -std=c++11 -fvisibility=hidden -O3 -DDLLEXT="\".so\"" $CPPFLAGS \
            -I"$(pwd)" "$WORK/$CLSNAME.cpp" -o "$WORK/$CLSNAME.meta"
          "$WORK/$CLSNAME.meta" > "$WORK/$CLSNAME.lv2/$CLSNAME.ttl"

          printf '%s\n' \
            '@prefix doap: <http://usefulinc.com/ns/doap#> .' \
            '@prefix foaf: <http://xmlns.com/foaf/0.1/> .' \
            '@prefix lv2:  <http://lv2plug.in/ns/lv2core#> .' \
            '@prefix rdfs: <http://www.w3.org/2000/01/rdf-schema#> .' \
            '' \
            "<https://faustlv2.bitbucket.io/$CLSNAME>" \
            '    a lv2:Plugin ;' \
            "    lv2:binary <$CLSNAME.so> ;" \
            "    rdfs:seeAlso <$CLSNAME.ttl> ." \
            > "$WORK/$CLSNAME.lv2/manifest.ttl"

          cp "$WORK/$CLSNAME.cpp" .
          docker run --rm --platform linux/arm64 -v "$PWD":/w -w /w \
            -e CPPFLAGS="$CPPFLAGS" -e CLSNAME="$CLSNAME" \
            alpine:3.20 sh -c '
              set -e
              apk add --no-cache build-base lv2-dev boost-dev
              g++ -shared -std=c++11 -fvisibility=hidden -O3 -mcpu=cortex-a72 -fPIC -DDLLEXT="\".so\"" $CPPFLAGS \
                -I. "$CLSNAME.cpp" -o "$CLSNAME.so"
              file "$CLSNAME.so"
            '
          cp "$CLSNAME.so" "$WORK/$CLSNAME.lv2/"

          ls -R "$WORK/$CLSNAME.lv2" || (echo "::error::no .lv2 bundle produced" && exit 1)
          mkdir -p "$GITHUB_WORKSPACE/effects/home"
          cp -r "$WORK/$CLSNAME.lv2" "$GITHUB_WORKSPACE/effects/home/"

      - name: Validate the LV2 bundle
        run: |
          sudo apt-get install -y lv2-dev lilv-utils || true
          cd effects/home
          find . -name '*.ttl' -print
          if command -v lv2_validate >/dev/null; then
            lv2_validate $(find . -name 'manifest.ttl') || echo "::warning::lv2_validate reported issues"
          fi

      - name: Upload guitar+lofi-fx LV2 bundle
        uses: actions/upload-artifact@v4
        with:
          name: guitar-lofi-fx-lv2
          path: effects/home/*.lv2
          if-no-files-found: error
          retention-days: 3

  resonode-lv2:
    runs-on: ubuntu-24.04-arm
    timeout-minutes: 40
    steps:
      - uses: actions/checkout@v4

      - name: Install Faust (host codegen only -- see the cross-compile step below)
        timeout-minutes: 8
        run: |
          sudo apt-get update
          sudo apt-get install -y faust lv2-dev pkg-config build-essential libboost-dev

      - name: Build resonode_synth.dsp as its own musl/aarch64 LV2 bundle
        timeout-minutes: 15
        run: |
          set -e
          FAUSTLIB=$(dirname "$(ls -f /usr/share/faust/lv2ui.cpp /usr/local/share/faust/lv2ui.cpp 2>/dev/null | tail -1)")
          cd effects/home/faust
          CLSNAME=resonode
          WORK=/tmp/lv2work
          mkdir -p "$WORK/$CLSNAME.lv2"

          faust -i -a "$FAUSTLIB/lv2.cpp" -vec -fun -dfs -vs 32 -nvi -ct 0 -cn "$CLSNAME" resonode_synth.dsp -o "$WORK/$CLSNAME.cpp"

          CPPFLAGS="-DPLUGIN_URI=\"https://faustlv2.bitbucket.io/$CLSNAME\" -DFAUST_META=1 -DFAUST_MIDICC=1 -DFAUST_MTS=1 -DFAUST_UI=0 -DVOICE_CTRLS=1 -DNVOICES=-1"

          g++ -std=c++11 -fvisibility=hidden -O3 -DDLLEXT="\".so\"" $CPPFLAGS \
            -I"$(pwd)" "$WORK/$CLSNAME.cpp" -o "$WORK/$CLSNAME.meta"
          "$WORK/$CLSNAME.meta" > "$WORK/$CLSNAME.lv2/$CLSNAME.ttl"

          printf '%s\n' \
            '@prefix doap: <http://usefulinc.com/ns/doap#> .' \
            '@prefix foaf: <http://xmlns.com/foaf/0.1/> .' \
            '@prefix lv2:  <http://lv2plug.in/ns/lv2core#> .' \
            '@prefix rdfs: <http://www.w3.org/2000/01/rdf-schema#> .' \
            '' \
            "<https://faustlv2.bitbucket.io/$CLSNAME>" \
            '    a lv2:Plugin ;' \
            "    lv2:binary <$CLSNAME.so> ;" \
            "    rdfs:seeAlso <$CLSNAME.ttl> ." \
            > "$WORK/$CLSNAME.lv2/manifest.ttl"

          cp "$WORK/$CLSNAME.cpp" .
          docker run --rm --platform linux/arm64 -v "$PWD":/w -w /w \
            -e CPPFLAGS="$CPPFLAGS" -e CLSNAME="$CLSNAME" \
            alpine:3.20 sh -c '
              set -e
              apk add --no-cache build-base lv2-dev boost-dev
              g++ -shared -std=c++11 -fvisibility=hidden -O3 -mcpu=cortex-a72 -fPIC -DDLLEXT="\".so\"" $CPPFLAGS \
                -I. "$CLSNAME.cpp" -o "$CLSNAME.so"
              file "$CLSNAME.so"
            '
          cp "$CLSNAME.so" "$WORK/$CLSNAME.lv2/"

          ls -R "$WORK/$CLSNAME.lv2" || (echo "::error::no .lv2 bundle produced" && exit 1)
          mkdir -p "$GITHUB_WORKSPACE/effects/resonode"
          cp -r "$WORK/$CLSNAME.lv2" "$GITHUB_WORKSPACE/effects/resonode/"

      - name: Validate the LV2 bundle
        run: |
          sudo apt-get install -y lv2-dev lilv-utils || true
          cd effects/resonode
          find . -name '*.ttl' -print
          if command -v lv2_validate >/dev/null; then
            lv2_validate $(find . -name 'manifest.ttl') || echo "::warning::lv2_validate reported issues"
          fi

      - name: Upload Resonode LV2 bundle
        uses: actions/upload-artifact@v4
        with:
          name: resonode-lv2
          path: effects/resonode/*.lv2
          if-no-files-found: error
          retention-days: 3

  pitchtracker-lv2:
    runs-on: ubuntu-24.04-arm
    timeout-minutes: 40
    steps:
      - uses: actions/checkout@v4

      - name: Install Faust (host codegen only -- see the cross-compile step below)
        timeout-minutes: 8
        run: |
          sudo apt-get update
          sudo apt-get install -y faust lv2-dev pkg-config build-essential libboost-dev

      - name: Build pitchtracker_ac.dsp as its own musl/aarch64 LV2 bundle
        timeout-minutes: 15
        run: |
          set -e
          FAUSTLIB=$(dirname "$(ls -f /usr/share/faust/lv2ui.cpp /usr/local/share/faust/lv2ui.cpp 2>/dev/null | tail -1)")
          cd effects/pitchtracker-src
          CLSNAME=pitchtracker
          WORK=/tmp/lv2work
          mkdir -p "$WORK/$CLSNAME.lv2"

          faust -i -a "$FAUSTLIB/lv2.cpp" -vec -fun -dfs -vs 32 -nvi -ct 0 -cn "$CLSNAME" pitchtracker_ac.dsp -o "$WORK/$CLSNAME.cpp"

          CPPFLAGS="-DPLUGIN_URI=\"https://faustlv2.bitbucket.io/$CLSNAME\" -DFAUST_META=1 -DFAUST_MIDICC=1 -DFAUST_MTS=1 -DFAUST_UI=0 -DVOICE_CTRLS=1 -DNVOICES=-1"

          g++ -std=c++11 -fvisibility=hidden -O3 -DDLLEXT="\".so\"" $CPPFLAGS \
            -I"$(pwd)" "$WORK/$CLSNAME.cpp" -o "$WORK/$CLSNAME.meta"
          "$WORK/$CLSNAME.meta" > "$WORK/$CLSNAME.lv2/$CLSNAME.ttl"

          printf '%s\n' \
            '@prefix doap: <http://usefulinc.com/ns/doap#> .' \
            '@prefix foaf: <http://xmlns.com/foaf/0.1/> .' \
            '@prefix lv2:  <http://lv2plug.in/ns/lv2core#> .' \
            '@prefix rdfs: <http://www.w3.org/2000/01/rdf-schema#> .' \
            '' \
            "<https://faustlv2.bitbucket.io/$CLSNAME>" \
            '    a lv2:Plugin ;' \
            "    lv2:binary <$CLSNAME.so> ;" \
            "    rdfs:seeAlso <$CLSNAME.ttl> ." \
            > "$WORK/$CLSNAME.lv2/manifest.ttl"

          cp "$WORK/$CLSNAME.cpp" .
          docker run --rm --platform linux/arm64 -v "$PWD":/w -w /w \
            -e CPPFLAGS="$CPPFLAGS" -e CLSNAME="$CLSNAME" \
            alpine:3.20 sh -c '
              set -e
              apk add --no-cache build-base lv2-dev boost-dev
              g++ -shared -std=c++11 -fvisibility=hidden -O3 -mcpu=cortex-a72 -fPIC -DDLLEXT="\".so\"" $CPPFLAGS \
                -I. "$CLSNAME.cpp" -o "$CLSNAME.so"
              file "$CLSNAME.so"
            '
          cp "$CLSNAME.so" "$WORK/$CLSNAME.lv2/"

          ls -R "$WORK/$CLSNAME.lv2" || (echo "::error::no .lv2 bundle produced" && exit 1)
          mkdir -p "$GITHUB_WORKSPACE/effects/pitchtracker"
          cp -r "$WORK/$CLSNAME.lv2" "$GITHUB_WORKSPACE/effects/pitchtracker/"

      - name: Validate the LV2 bundle
        run: |
          sudo apt-get install -y lv2-dev lilv-utils || true
          cd effects/pitchtracker
          find . -name '*.ttl' -print
          if command -v lv2_validate >/dev/null; then
            lv2_validate $(find . -name 'manifest.ttl') || echo "::warning::lv2_validate reported issues"
          fi

      - name: Upload PitchTracker LV2 bundle
        uses: actions/upload-artifact@v4
        with:
          name: pitchtracker-lv2
          path: effects/pitchtracker/*.lv2
          if-no-files-found: error
          retention-days: 3

  delayverb-lv2:
    runs-on: ubuntu-24.04-arm
    timeout-minutes: 40
    steps:
      - uses: actions/checkout@v4

      - name: Install Faust (host codegen only -- see the cross-compile step below)
        timeout-minutes: 8
        run: |
          sudo apt-get update
          sudo apt-get install -y faust lv2-dev pkg-config build-essential libboost-dev

      - name: Build delayverb.dsp as its own musl/aarch64 LV2 bundle
        timeout-minutes: 15
        run: |
          set -e
          FAUSTLIB=$(dirname "$(ls -f /usr/share/faust/lv2ui.cpp /usr/local/share/faust/lv2ui.cpp 2>/dev/null | tail -1)")
          cd effects/delayverb-src
          CLSNAME=delayverb
          WORK=/tmp/lv2work
          mkdir -p "$WORK/$CLSNAME.lv2"

          faust -i -a "$FAUSTLIB/lv2.cpp" -vec -fun -dfs -vs 32 -nvi -ct 0 -cn "$CLSNAME" delayverb.dsp -o "$WORK/$CLSNAME.cpp"

          CPPFLAGS="-DPLUGIN_URI=\"https://faustlv2.bitbucket.io/$CLSNAME\" -DFAUST_META=1 -DFAUST_MIDICC=1 -DFAUST_MTS=1 -DFAUST_UI=0 -DVOICE_CTRLS=1 -DNVOICES=-1"

          g++ -std=c++11 -fvisibility=hidden -O3 -DDLLEXT="\".so\"" $CPPFLAGS \
            -I"$(pwd)" "$WORK/$CLSNAME.cpp" -o "$WORK/$CLSNAME.meta"
          "$WORK/$CLSNAME.meta" > "$WORK/$CLSNAME.lv2/$CLSNAME.ttl"

          printf '%s\n' \
            '@prefix doap: <http://usefulinc.com/ns/doap#> .' \
            '@prefix foaf: <http://xmlns.com/foaf/0.1/> .' \
            '@prefix lv2:  <http://lv2plug.in/ns/lv2core#> .' \
            '@prefix rdfs: <http://www.w3.org/2000/01/rdf-schema#> .' \
            '' \
            "<https://faustlv2.bitbucket.io/$CLSNAME>" \
            '    a lv2:Plugin ;' \
            "    lv2:binary <$CLSNAME.so> ;" \
            "    rdfs:seeAlso <$CLSNAME.ttl> ." \
            > "$WORK/$CLSNAME.lv2/manifest.ttl"

          cp "$WORK/$CLSNAME.cpp" .
          docker run --rm --platform linux/arm64 -v "$PWD":/w -w /w \
            -e CPPFLAGS="$CPPFLAGS" -e CLSNAME="$CLSNAME" \
            alpine:3.20 sh -c '
              set -e
              apk add --no-cache build-base lv2-dev boost-dev
              g++ -shared -std=c++11 -fvisibility=hidden -O3 -mcpu=cortex-a72 -fPIC -DDLLEXT="\".so\"" $CPPFLAGS \
                -I. "$CLSNAME.cpp" -o "$CLSNAME.so"
              file "$CLSNAME.so"
            '
          cp "$CLSNAME.so" "$WORK/$CLSNAME.lv2/"

          ls -R "$WORK/$CLSNAME.lv2" || (echo "::error::no .lv2 bundle produced" && exit 1)
          mkdir -p "$GITHUB_WORKSPACE/effects/delayverb"
          cp -r "$WORK/$CLSNAME.lv2" "$GITHUB_WORKSPACE/effects/delayverb/"

      - name: Validate the LV2 bundle
        run: |
          sudo apt-get install -y lv2-dev lilv-utils || true
          cd effects/delayverb
          find . -name '*.ttl' -print
          if command -v lv2_validate >/dev/null; then
            lv2_validate $(find . -name 'manifest.ttl') || echo "::warning::lv2_validate reported issues"
          fi

      - name: Upload DelayVerb LV2 bundle
        uses: actions/upload-artifact@v4
        with:
          name: delayverb-lv2
          path: effects/delayverb/*.lv2
          if-no-files-found: error
          retention-days: 3
