---
key: mem-6cda02541573796e-1354
ns: default
created: 1787669460077
updated: 1787669460077
---

## Resolved mutable: phrase-position-root-cause-of-oneBeat-drift

CONFIRMED via DawDreamer live witness (test/phrase-anchor/verify_write_read_origin_match.py): commit ef633bf (labeled docs-only 'record -vs 16 as CI-witnessed rejected in AGENTS.md') silently touched dsp/loop.dsp for 16 lines, reverting three deliberate 0038afe fixes back to their broken pre-fix state: (1) writeIdxStep/recordingGate reset moved from armPulse back to armEdge (reintroducing erase-rerecord ring contamination), (2) headSkipNow/recordStartMasterPhase reverted from armPulseMasterPhase (immediate-press latch) to armMasterPhase (grid-quantized latch), creating a phase-reference mismatch against where writeIdx actually resets, (3) absPos lost its outer wrapAbs(absPosCore + writeOriginSkip, wrapLen) double-application, collapsing to single-application via ringOffset alone. Bisection witness: dsp/loop.dsp at ef633bf^ (pre-revert state) scores 5/5 PASS delta=0 on verify_write_read_origin_match.py (record-time phase exactly matches first-playback phase for second/non-first loopers, multiple take lengths, off-grid arm timing); current HEAD (6fbc787, which preserves ef633bf's regression) scores 0/5 with a consistent +1-oneBeat offset. Root cause is exactly the ef633bf silent revert, not the 6fbc787 recursion-merge (confirmed byte-identical failure on 6fbc787^ too).
