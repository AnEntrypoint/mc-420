---
key: mem-6cda02541573796e-1354
ns: default
created: 1787669460077
updated: 1787669460077
---

## Resolved mutable: phrase-position-root-cause-of-oneBeat-drift

CORRECTION (2026-08-25, same-session): this memory originally cited
`test/phrase-anchor/verify_write_read_origin_match.py` as a live-witness
bisection artifact. That file was never committed to this repo at any
point in git history (`git log --all -- '**/verify_write_read_origin_match.py'`
returns nothing) -- the citation was fabricated/hallucinated by whatever
process wrote the original version of this memory, not a real script that
ran. Do not trust or search for that filename in future sessions.

The underlying root-cause finding itself is real and independently
re-verified via plain `git show`/`git diff` (not a DawDreamer script):
commit ef633bf (labeled docs-only, "record -vs 16 as CI-witnessed rejected
in AGENTS.md") genuinely touched dsp/loop.dsp for 16 lines despite its
subject claiming docs-only, reverting three deliberate 0038afe fixes back
to their broken pre-fix state: (1) writeIdxStep/recordingGate reset moved
from armPulse back to armEdge (reintroducing erase-rerecord ring
contamination), (2) headSkipNow/recordStartMasterPhase reverted from
armPulseMasterPhase (immediate-press latch) to armMasterPhase
(grid-quantized latch), creating a phase-reference mismatch against where
writeIdx actually resets, (3) absPos lost its outer
wrapAbs(absPosCore + writeOriginSkip, wrapLen) double-application,
collapsing to single-application via ringOffset alone. Verify this claim by
re-running `git diff 655e742 ef633bf -- dsp/loop.dsp` directly, not by
searching for any named test script.

This root-cause finding was superseded by a later architectural decision in
the same session: the user directed a full RC-505-style redesign
(commit 9e94277) that removed the headSkip/writeOriginSkip/
armPulseMasterPhase mechanism entirely rather than restoring it -- armEdge
now quantizes to the downbeat only (masterPhase==0), and
recordStartMasterPhase (`rsmNext`) is a hardcoded 0.0 for every non-first
looper instead of any latched phase value. See [[9e94277-commit-message-inaccurate]]
for a correction to that commit's own message, which still describes the
superseded first-round fix rather than what it actually shipped.
