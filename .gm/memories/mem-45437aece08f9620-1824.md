---
key: mem-45437aece08f9620-1824
ns: default
created: 1787673070095
updated: 1787673070095
---

Resolved mutable: 9e94277-commit-message-inaccurate

Commit 9e94277 ("fix: restore ef633bf-regressed phase-anchor semantics in dsp/loop.dsp") has a commit message that does NOT match its own diff. The message describes restoring armPulseMasterPhase-latched phase reference and double-applied writeOriginSkip in absPos -- but the actual committed dsp/loop.dsp (verified via `git show 9e94277:dsp/loop.dsp`) contains a DIFFERENT, later design: rsmNext hardcoded to 0.0 for every non-first looper, with headSkipNow/writeOriginSkip/armPulseMasterPhase removed entirely. This happened because the commit was created (likely by a background fork/daemon process mid-session) capturing the final RC-505-redesigned working-tree state, but generating its message from earlier PRD-row/mutable text written before the RC-505 redesign decision. The commit's PRD-row-resolution line at the bottom (citing fix-ringOffset-to-match-record-time-phase) is the same stale text.

Found by an independent adversarial-review Agent dispatched per this project's gm-skill DECIDE-phase discipline (multi-file diffs must be reviewed by a reviewer blind to the implementer's own reasoning, prompted only to refute). The review also found the diff's actual DSP LOGIC is correct (hand-traced Faust tuple arity, armEdge/rsmNext/widxNext semantics, masterLen<0.5-to->=0.5 transition safety) -- only the commit message's own prose is wrong, not the shipped behavior. Never trust a commit message in this repo as ground truth for WHAT changed without diffing the actual commit; this is the same class of lesson as [[no-comments-rule]]'s "never trust an in-repo comment" but for commit messages specifically. A corrective follow-up commit (documenting the actual final design accurately) should be made if this commit's message is ever cited as documentation.
