---
key: mem-0cab0732beb4098e-967
ns: default
created: 1788640494891
updated: 1788640494891
---

serve-netboot-win.js resolves SERVER_IP ONCE at startup and bakes it into the netboot root cmdline.txt (alpine_repo/modloop/apkovl URLs). Restarting the server rebinds sockets to the new address but does NOT rewrite cmdline.txt, so the stale IP keeps shipping to the Pi. Diagnostic signature: TFTP serves the whole firmware+kernel chain fine (option 66 live and correct) then ZERO HTTP hits, and the Pi stalls in initramfs -- it pings with TTL=64 but port 22 is refused because Alpine never comes up so sshd never starts. DISTINCT from AGENTS.md documented 'option 66 points at a dead address', which shows zero TFTP reads instead. Fix: rebuild the root with NETBOOT_SERVER correct, then power-cycle -- the udp/4446 REBOOT path cannot help because aloop never started. Trigger here: Ethernet link was down at server startup so resolveServerIp auto-detected the Wi-Fi address. Verify: cat .netboot-serve/cmdline.txt | tr ' ' '
' | grep -E 'apkovl|modloop|alpine_repo'.
